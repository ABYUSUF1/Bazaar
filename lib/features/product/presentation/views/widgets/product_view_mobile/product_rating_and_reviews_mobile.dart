import 'package:flutter/material.dart';

class ProductRatingAndReviewsMobile extends StatelessWidget {
  const ProductRatingAndReviewsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [],
    );
  }
}
